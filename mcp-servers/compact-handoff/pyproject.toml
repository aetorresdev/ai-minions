[project]
name = "compact-handoff"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    # Stay on SDK v1 until servers migrate FastMCP → MCPServer (mcp 2.x).
    "mcp>=1.28.1,<2",
    "httpx>=0.27.0",
]
