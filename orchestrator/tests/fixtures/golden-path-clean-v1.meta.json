{
  "meta_version": 1,
  "fixture_file": "golden-path-clean-v1.jsonl",
  "description": "Synthetic fixed clocks in the JSONL. CI fails if wall span or agent durations drift without an intentional baseline refresh (set UPDATE_GOLDEN_META=1 after editing the trace).",
  "bounds": {
    "wall_span_ts_ms": {
      "min": 4000,
      "max": 4000,
      "from": "session_end.ts_ms - session_start.ts_ms"
    },
    "agent_done_duration_ms_total": {
      "min": 10,
      "max": 10,
      "from": "sum(agent_done.duration_ms)"
    },
    "cost_usd_explain_sum": {
      "expect_absent": true,
      "from": "deriveExplain(rows).cost_usd omitted when no line carries cost_usd"
    }
  }
}
