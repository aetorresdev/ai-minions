# IDE
.vscode/
.idea/
node_modules/
# Debug / runtime
debug/
*.log
session-env/
sessions/
shell-snapshots/
todos/
ide/
file-history/
backups/

# Python packaging (local editable installs)
*.egg-info/

# Cache
cache/
__pycache__/
*.pyc
*.pyo
.cache/
.pytest_cache/

# Downloads
downloads/
telemetry/

# System files
.DS_Store
Thumbs.db

# Credentials and secrets
.credentials.json
.env
.env.local
secrets/
.ai-minions/
# Orchestrator L2 state (on-disk authority; may live under repo if ORCHESTRATOR_STATE_ROOT points here)
.state/

# Claude Code runtime data (personal, not repo content)
projects/
history.jsonl
policy-limits.json
metrics/
report-bundles/
# Per-project flow-metrics hook state (not metrics/ trace authority)
.claude/flow-hook-state/
.claude/worktrees/
.claude/worktree-binding.json
.claude/run-workdir-contract.json
.claude/run-artifacts/
plugins/
tasks/
# MCP server virtualenvs (lock files are committed for reproducible uv sync + release scan)
mcp-servers/**/.venv/
mcp-servers/**/node_modules/

# Settings (contains local paths and hooks — copy from settings.example.json)
settings.json

# Work-in-progress specs, ADRs, and internal follow-ups — not ready for public repo
docs/specs/
docs/adr/
docs/FOLLOW_UPS.md

# Local-only product backlog / resolved archive — NEVER commit (personal planning)
AI-Minions*.md

# Groomed backlog (local SoT — do not commit)
docs/ai-minions-backlog-groomed.md
docs/backlog-open-specs.md
docs/archive/ai-minions-backlog-groomed-*.md
docs/archive/backlog-*.md

# Claude session snapshots (local; not repo content)
.claude/state/
.claude/snapshots/
state/*
usage-data/*
stats-cache.json
mcp-needs-auth-cache.json
.cursor/*
scripts/clean-ai-commit-trails.sh
scripts/sync-trello-backlog.py
scripts/slim-backlog-groomed.py
scripts/slim-backlog-groomed-phase2.py
paste-cache/

# Cohort UX discovery live friction logs (operator private — example file stays versioned)
docs/how-to/evidence/cohort-friction-live.jsonl
docs/how-to/evidence/cohort-friction-*.jsonl
!docs/how-to/evidence/cohort-friction-log.example.jsonl
